Need to figure out how to filter the array generated by the user selections. Or need to figure out how to select a winner using the id's of each grid space
the way I had it written isn't working, it's pulling 