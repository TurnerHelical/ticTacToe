function createPlayer(name) {
    const getName = () => 
}

function dom(el1, el2) {
    const findElement = () => document.querySelector(`${el1}`);
    const createElement = () => document.createElement(`${el1}`);
    const appendElement = () => el1.appendChild(`${el2}`);

}